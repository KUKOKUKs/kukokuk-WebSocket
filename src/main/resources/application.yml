server:
  port: 8081          # WebSocket 전용 서버 포트

spring:
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  session:
    store-type: redis

api:
  server:
    base-url: ${API_BASE_URL:http://localhost:8080}